setup:
  addons:
    - plan: heroku-postgresql:essential-0
      as: DATABASE
    - plan: heroku-redis:basic-0
      as: REDIS
    - plan: papertrail:choklad

build:
  docker:
    web: Dockerfile

run:
  web: /entrypoint.sh
setup:
  addons:
    - plan: heroku-postgresql:mini
      as: DATABASE
    - plan: heroku-redis:mini
      as: REDIS
    - plan: papertrail:choklad

build:
  docker:
    web: Dockerfile

run:
  web: /entrypoint.sh
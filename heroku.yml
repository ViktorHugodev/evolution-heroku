setup:
  addons:
    - plan: heroku-postgresql:essential-0
      as: DATABASE
    - plan: heroku-redis:mini
      as: REDIS

build:
  docker:
    web: Dockerfile

run:
  web: /evolution/entrypoint.sh